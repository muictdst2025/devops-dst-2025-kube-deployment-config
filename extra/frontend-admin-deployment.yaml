apiVersion: v1
kind: Service
metadata:
  name: <group-name>-frontend-admin-service
  namespace: <group-name>-app
spec:
  selector:
    app: <group-name>-frontend-admin
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP


---


apiVersion: apps/v1
kind: Deployment
metadata:
  name: <group-name>-frontend-admin-deployment
  namespace: <group-name>-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: <group-name>-frontend-admin
  template:
    metadata:
      labels:
        app: <group-name>-frontend-admin
    spec:
      containers:
        - name: <group-name>-frontend-admin
          image: <dockerhub-username>/<frontend-admin-image-name>:<tag>
          ports:
            - containerPort: 3000
          env:

            # ------------------------------------------------------
            # Option: 1 — ถ้า frontend ใช้ env API
            # ให้ uncomment ตัวนี้ แล้วแก้เป็น path ของกลุ่มตามนี้
            # ------------------------------------------------------
            # - name: NEXT_PUBLIC_API_URL
            #   value: "https://muict.app/<group-name>-backend"

            # - name: VITE_API_URL
            #   value: "https://muict.app/<group-name>-backend"

            # ------------------------------------------------------
            # Option: 2 — ถ้า frontend hardcode localhost
            # ต้องไปแก้ใน code เองเป็น:
            #   https://muict.app/<group-name>-backend
            # ไม่ต้องตั้ง env ที่นี่
            # แล้วก็ปล่อย comment ไว้แบบนี้ได้เลย 
            # ------------------------------------------------------


